{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun i --frozen-lockfile --production=false && cd docs && bun i --frozen-lockfile --production=false",
  "buildCommand": "cd docs && bun run build",
  "outputDirectory": "docs/.next"
}
